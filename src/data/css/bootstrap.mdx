import { Icon } from '@iconify/react'

<Icon icon="devicon:bootstrap" height={14} style={{display: 'inline', verticalAlign: 'middle'}} /> Bootstrap is a popular CSS framework that provides prebuilt components and a responsive grid system.

**CDN installation:**

```html
<link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
```

**Example usage:**

```html
<button class="btn btn-primary">Submit</button>
```
