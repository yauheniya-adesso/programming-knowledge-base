import LanguagePage from '../components/LanguagePage';
import { htmlContent } from '../data/html-content';

const HTML = () => {
  return <LanguagePage content={htmlContent} />;
};

export default HTML;
